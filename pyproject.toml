[project]
name = "quant-trading-system-case-study"
version = "0.1.0"
description = "Public case-study and safe demo for a private AI-assisted Python quant trading system."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
